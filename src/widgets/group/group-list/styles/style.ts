import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
	groupList: {
		flex: 1,
		paddingTop: 20
	},
	withoutCounters: {
		alignItems: 'center',
		paddingInline: 5,
		textAlign: 'center'
	}
});

export default style;
