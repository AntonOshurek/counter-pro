import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
	GroupCounterList: {
		flex: 1
	},
	withoutCounters: {
		alignItems: 'center',
		paddingInline: 5,
		textAlign: 'center',
		paddingTop: 20
	}
});

export default style;
