//NATIVE
import { View } from 'react-native';
//FEATURES
import { GroupsList } from '../../features';

const GroupsWidget = () => {
	return (
		<View>
			<GroupsList />
		</View>
	);
};

export default GroupsWidget;
