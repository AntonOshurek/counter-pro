import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  GroupCounterList: {
    flex: 1,
    paddingTop: 20,
    gap: 20
  },
  list: {
    flex: 1
  }
});

export default style;
