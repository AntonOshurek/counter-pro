//NATIVE
import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
	createdDate: {
		flexDirection: 'row',
		alignItems: 'center',
		justifyContent: 'space-between',
		gap: 20
	}
});

export { style };
