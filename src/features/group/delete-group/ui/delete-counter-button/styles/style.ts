//NATIVE
import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
	deleteGroupButton: {
		flexDirection: 'row',
		gap: 20,
		alignItems: 'center',
		justifyContent: 'space-between'
	}
});

export { style };
