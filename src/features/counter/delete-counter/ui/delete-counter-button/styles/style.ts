//NATIVE
import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
	deleteCounterButton: {
		flexDirection: 'row',
		gap: 20,
		alignItems: 'center',
		justifyContent: 'space-between'
	}
});

export { style };
