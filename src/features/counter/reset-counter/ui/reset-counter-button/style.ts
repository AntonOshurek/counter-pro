import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
	resetCounterButton: {
		flexDirection: 'row',
		alignItems: 'center',
		justifyContent: 'space-between'
	}
});

export { style };
