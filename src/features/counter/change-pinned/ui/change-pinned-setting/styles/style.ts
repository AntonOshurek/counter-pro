import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
	changePinnedSetting: {
		flexDirection: 'row',
		alignItems: 'center',
		justifyContent: 'space-between'
	}
});

export { style };
